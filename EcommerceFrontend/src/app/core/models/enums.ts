export enum EStatus {
  pending = 'pending',
  preparing = 'preparing',
  shipped = 'shipped',
  delivered = 'delivered',
  cancelled = 'cancelled',
  rejected = 'rejected',
}

export enum ERole {
  user = 'user',
  admin = 'admin',
  superAdmin = 'super-admin',
}
